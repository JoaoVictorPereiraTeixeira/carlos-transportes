// @flow 
import { Container } from '@material-ui/core';
import * as React from 'react';
type Props = {
    
};

 const FormMudancas: React.FC = (props: any) => {
    return (
        <Container>
            Show
        </Container>
    );
};

export default FormMudancas;